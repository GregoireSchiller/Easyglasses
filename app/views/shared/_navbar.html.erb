<div class="navbar-wagon">
  <!-- Logo -->
  <div class="logo-name">
    <div class="logo">
      <%= link_to root_path, class: "navbar-wagon-brand" do %>
        <%= image_tag "Logo_6.svg" %>
      <% end %>
    </div>
    <div class="name">
      <div style="color:white"><p><strong style="font-weight:400;">EasyGlasses</strong></p></div>
    </div>
  </div>
  <!-- Right Navigation -->
  <div class="navbar-wagon-right hidden-xs hidden-sm">

    <!-- Different navigation if login or not -->
    <% if user_signed_in? %>

      <!-- Profile picture and dropdown -->
      <div class="navbar-wagon-item">
        <div class="dropdown">
          <div class="dropdown-toggle user-menu" data-toggle="dropdown">
            <%= current_user.first_name %> <span class="caret"></span>
          </div>
          <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
            <li>
              <%= link_to user_path(current_user) do %>
                <i class="fa fa-user"></i> <%= t ".profile", default: "Profile" %>
              <% end %>
            </li>
            <li>
              <%= link_to root_path do %>
                <i class="fa fa-home"></i>  <%= t ".profile", default: "Home" %>
              <% end %>
            </li>
            <li>
              <%= link_to destroy_user_session_path, method: :delete do %>
                <i class="fa fa-sign-out"></i>  <%= t ".sign_out", default: "Log out" %>
              <% end %>
            </li>
          </ul>
        </div>
      </div>

    <% else %>

      <!-- Login text link -->
      <button type="button" class="btn btn-primary navbar-wagon-item navbar-wagon-link" data-toggle="modal" data-target="#myModal">
      Login
      </button>
      <%= render "log_in/log_in_modal" %>
     <!-- link_to t(".sign_in", default: "Login"), new_user_session_path, class: "navbar-wagon-item navbar-wagon-link" %> -->


    <% end %>

    <!-- Button (call-t o-action) -->
    <% if current_user != nil %>
      <%= link_to exams_path, method: "post", class: "navbar-wagon-button-container" do %>
        <div class="navbar-wagon-item navbar-wagon-button">
          Test your sight!
        </div>
      <% end %>
    <% else %>
      <%= link_to new_user_registration_path, class: "navbar-wagon-button-container" do %>
        <div class="navbar-wagon-item navbar-wagon-button">
          Test your sight!
        </div>
      <% end %>
    <% end %>
  </div>
</div>
